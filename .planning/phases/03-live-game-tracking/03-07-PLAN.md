---
phase: 03-live-game-tracking
plan: 07
type: execute
wave: 1
depends_on: [03-01, 03-02, 03-03, 03-05, 03-06]
files_modified:
  - stonereader/services/_engine.py
  - tests/test_services/test_engine_live_state.py
  - .planning/phases/03-live-game-tracking/03-HUMAN-UAT.md
autonomous: true
gap_closure: true
requirements: []
requirements_completed: [LIVE-02, LIVE-06, LIVE-07, LIVE-08]
must_haves:
  truths:
    - "Feeding any of the 4 captured Power.log fixtures (match_start, mid_game, game_end, reconnect) through Parser+Engine yields a GameState whose player_deck contains at least 1 GameEntity row (proving _refresh_state rebuilt it from _entities; the post-replay count is the live remaining-deck size, which is < 30 once cards have been drawn)."
    - "After the same fixture replay, state.player_hero.hero_class is non-empty (e.g. 'MAGE', 'WARRIOR') matching the local player's hero card_class."
    - "After replaying mid_game.log, state.player_max_mana has advanced past 1 (every TURN tag bumps RESOURCES) and state.player_mana = RESOURCES − RESOURCES_USED on demand."
    - "state.player_deck_count and state.opponent_deck_count reflect the live count of entities with ZONE==DECK per controller (not zero) by mid-game."
    - "HUMAN-UAT B-series prerequisites explicitly document that the mana line is blank with no game in progress (LIVE-07) and that full-panel walk requires NVDA Say-All / browse mode (B2/B4)."
  artifacts:
    - path: "tests/test_services/test_engine_live_state.py"
      provides: "Captured-fixture regression lock for engine GameState publication (player_deck rebuild, hero resolution, mana tags, deck counts)."
      contains: "test_player_deck_rebuilt_from_entities, test_hero_class_resolved, test_mana_tags_advance, test_deck_counts_track_zone"
      min_lines: 80
    - path: "stonereader/services/_engine.py"
      provides: "_resolve_heroes helper + extended _refresh_state (player_deck + deck counts) + extended _on_tag_change (RESOURCES, RESOURCES_USED)."
      exports: ["GameEngine"]
      contains: "_resolve_heroes"
    - path: ".planning/phases/03-live-game-tracking/03-HUMAN-UAT.md"
      provides: "Two prerequisite notes covering mana-blank-on-no-game and Say-All-for-full-walk."
      contains: "Prerequisite"
  key_links:
    - from: "GameEngine._on_create_game"
      to: "GameEngine._resolve_heroes"
      via: "called after _record_entity loop populates _entities, before constructing initial GameState"
      pattern: "_resolve_heroes\\("
    - from: "GameEngine._record_entity"
      to: "GameEngine._resolve_heroes"
      via: "FullEntity / SHOW_ENTITY may bring HERO_ENTITY tags after CREATE_GAME — re-resolve heroes opportunistically when CARDTYPE==HERO observed"
      pattern: "_resolve_heroes\\("
    - from: "GameEngine._on_tag_change"
      to: "self._current_state (RESOURCES + RESOURCES_USED branches)"
      via: "dataclasses.replace updating player_mana / player_max_mana / opponent_mana / opponent_max_mana keyed on entity controller"
      pattern: "RESOURCES_USED|RESOURCES"
    - from: "GameEngine._refresh_state"
      to: "state.player_deck"
      via: "iterate _entities for ZONE==DECK and CONTROLLER==_friendly_player_id, build GameEntity tuple sorted by zone_position"
      pattern: "player_deck=tuple"
    - from: "GameEngine._refresh_state"
      to: "state.player_deck_count + state.opponent_deck_count"
      via: "per-controller count of _entities where ZONE==DECK"
      pattern: "player_deck_count="
---

<objective>
Close the engine-publication gap diagnosed in `.planning/debug/live-tracking-not-engaging-vs-innkeeper.md`. `GameEngine._refresh_state()` currently republishes only 5 of ~15 GameState fields; the live presenter consumes `state.player_deck`, `state.player_hero.hero_class`, `state.player_mana`, `state.player_deck_count` — all permanently empty/zero — so the LiveGamePanel cannot reflect anything in any real Hearthstone match. Three targeted engine extensions plus one captured-fixture integration test close LIVE-02 / LIVE-06 / LIVE-07 / LIVE-08 user-visible delivery.

Folds in the doc-only follow-up from `.planning/debug/live-game-panel-layout.md` (Gap 1) so HUMAN-UAT testers no longer mistake spec behavior (blank mana with no game; one-announcement on focus enter) for regressions.

Purpose: Make the LiveGamePanel reflect real game state vs the Innkeeper / any opponent, and close the test-coverage gap (`tests/test_live_game_presenter.py:87-127`'s `_make_state` helper currently masks engine-publication gaps because no integration test feeds real Power.log through the chain).

Output: Extended `_engine.py` with hero resolution + RESOURCES tag handling + `player_deck` / deck-count rebuild in `_refresh_state`. New `tests/test_services/test_engine_live_state.py` regression lock. Two prerequisite notes appended to `03-HUMAN-UAT.md`.
</objective>

<execution_context>
@C:/Users/andrew/code/stonereader/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/andrew/code/stonereader/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-live-game-tracking/03-UAT.md
@.planning/debug/live-tracking-not-engaging-vs-innkeeper.md
@.planning/debug/live-game-panel-layout.md
@.planning/phases/03-live-game-tracking/03-VALIDATION.md
@stonereader/services/_engine.py
@stonereader/services/_packets.py
@stonereader/services/_parser.py
@stonereader/models/game_state.py
@stonereader/presenters/live_game.py
@tests/test_services/conftest.py
@tests/test_services/test_engine.py
@tests/test_services/test_engine_friendly_player.py

<interfaces>
<!-- Key types and contracts the executor needs. Embedded so no codebase exploration is required. -->

From stonereader/services/_packets.py:
```python
@dataclass(frozen=True)
class CreateGamePacket(Packet):
    game_entity_id: int
    players: Tuple[Tuple[int, int, str, int, int], ...] = ()  # (entity_id, player_id, name, hi, lo)
    initial_tags: Dict[str, int] = field(default_factory=dict)

@dataclass(frozen=True)
class TagChangePacket(Packet):
    entity_id: int
    tag: str   # GameTag enum NAME (e.g. "ZONE", "CONTROLLER", "RESOURCES", "RESOURCES_USED")
    value: int  # raw int
    source_id: Optional[int] = None

@dataclass(frozen=True)
class FullEntityPacket(Packet):
    entity_id: int
    card_id: str
    tags: Dict[str, int] = field(default_factory=dict)  # tag NAMES → int values

@dataclass(frozen=True)
class ShowEntityPacket(Packet):
    entity_id: int
    card_id: str
    tags: Dict[str, int] = field(default_factory=dict)
```

From stonereader/models/game_state.py:
```python
@dataclass(frozen=True)
class Hero:
    id: str
    name: str
    health: int
    armor: int
    hero_power: str
    hero_class: str = ""   # "MAGE", "WARRIOR", etc. (matches Card.card_class)

@dataclass(frozen=True)
class GameEntity:
    entity_id: int
    card_id: str
    base_card: Optional[Card]
    name: str
    cost: int
    current_attack: int
    current_health: int
    card_type: str
    zone: str               # zone NAME ("HAND" / "DECK" / "PLAY")
    zone_position: int
    controller: int
    exhausted: bool = False
    enchantment_names: Tuple[str, ...] = ()
    tags: Dict[str, Any] = field(default_factory=dict)
    drawn_turn: int = -1
    creation_lineage: str = ""

@dataclass(frozen=True)
class GameState:
    turn: int
    active_player_id: int
    player_board: Tuple[GameEntity, ...]
    opponent_board: Tuple[GameEntity, ...]
    player_hand: Tuple[GameEntity, ...]
    opponent_hand: Tuple[Optional[GameEntity], ...]
    player_hero: Hero
    opponent_hero: Hero
    # ... player_weapon/secrets ...
    player_mana: int = 0
    player_max_mana: int = 0
    opponent_mana: int = 0
    opponent_max_mana: int = 0
    player_deck_count: int = 0
    opponent_deck_count: int = 0
    player_deck: Tuple[GameEntity, ...] = ()
    # ... player_played / opponent_played / player_drawn / opponent_drawn / starting_hand ...
```

From stonereader/models/card.py:
```python
@dataclass(frozen=True)
class Card:
    id: str
    dbf_id: int
    name: str
    cost: int
    # ...
    card_class: str   # "MAGE", "WARRIOR", "NEUTRAL", ...
    card_type: str    # "MINION", "SPELL", "WEAPON", "HERO", "HERO_POWER"
    # ...

class CardDatabase:
    def get_card_by_id(self, card_id: str) -> Optional[Card]: ...
```

From stonereader/services/_engine.py (existing internals available to the new code):
```python
class GameEngine:
    def __init__(self, card_db=None, clock=time.monotonic):
        self._card_db = card_db                  # already wired by tracker
        self._current_state: Optional[GameState] = None
        self._entities: Dict[int, dict] = {}     # entity_id -> {"ZONE": int, "CONTROLLER": int, "CARDTYPE": int, "card_id": str, ...}
        self._friendly_player_id: int = 1
        # ...

    def _lookup_card(self, card_id: str) -> Optional[Card]: ...   # safe; already exists
    def _record_entity(self, eid: int, card_id: str, tags: Dict[str,int]) -> None: ...
    def _refresh_state(self) -> None: ...   # bug: leaves player_deck/mana/deck_count/heroes stale
```

From hearthstone.enums (used in engine via `from hearthstone.enums import Zone`):
```python
Zone.DECK.value == 2
Zone.HAND.value == 3
Zone.PLAY.value == 1
CardType.HERO.value == 4   # NOTE: store/compare CARDTYPE tag value as int(CardType.HERO)
```
</interfaces>

<diagnosis_summary>
GameEngine._refresh_state (engine.py:562-609) republishes 5 fields:
  player_played, opponent_played, player_drawn, opponent_drawn, opponent_hand
GameEngine._on_create_game (engine.py:292-345) initializes 10+ fields to empty defaults:
  player_hero=empty (hero_class=""), opponent_hero=empty, player_deck=(), player_deck_count=0,
  opponent_deck_count=0, player_mana=0, player_max_mana=0, opponent_mana=0, opponent_max_mana=0,
  player_hand=(), player_board=(), opponent_board=()
GameEngine._on_tag_change (engine.py:347-395) handles:
  TURN, CURRENT_PLAYER, ZONE, PLAYSTATE, DAMAGE, MULLIGAN_STATE
But MISSES:
  RESOURCES, RESOURCES_USED — needed for player_mana / player_max_mana / opponent_mana / opponent_max_mana
And there is no hero-resolution path that reads HERO_ENTITY / CARDTYPE==HERO to find each player's hero entity.

Test gap that masked it (tests/test_live_game_presenter.py:87-127): `_make_state` helper constructs fully-populated synthetic GameState. Presenter unit tests pass against synthetic state; engine unit tests use synthetic packets; no test feeds real Power.log → Parser → Engine and asserts the engine publishes what the presenter consumes.

NUM_CARDS_IN_DECK is NOT a GameTag in hearthstone.enums (verified via `from hearthstone.enums import GameTag; GameTag['NUM_CARDS_IN_DECK']` raises KeyError). Deck count must be DERIVED in `_refresh_state` by counting `_entities` rows where `ZONE==int(Zone.DECK)` per controller.

In contrast, RESOURCES (id 26), RESOURCES_USED (id 25), HERO_ENTITY (id 27), CARDTYPE (id 202) ARE valid GameTag names AND appear in the captured fixtures (verified by grep on `tests/fixtures/log/mid_game.log`).
</diagnosis_summary>

</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing integration test for engine GameState publication</name>
  <files>tests/test_services/test_engine_live_state.py</files>
  <behavior>
    The test file feeds captured Power.log fixtures through real Parser + GameEngine (no MockGameTracker, no synthetic _make_state) and asserts the engine PUBLISHES the fields the live-game presenter consumes. The 4 tests below MUST fail on current `main` (HEAD before Task 2 lands) and pass after Task 2.

    Test 1 — `test_player_deck_rebuilt_from_entities`:
      - Loads `power_log_fixture("match_start.log")` (skips if absent — uses existing `power_log_fixture` from `tests/test_services/conftest.py`).
      - Construct `Parser()` and `GameEngine(card_db=load_real_card_db())` (load via the same path the engine tests already use — see `tests/test_services/test_engine.py` for precedent; if the existing engine tests pass `card_db=None` and rely on `_lookup_card` returning None, do the same here for the deck test where Card lookup is not strictly required, but the hero test below DOES need a real card_db — so this file MUST load the real CardDatabase via `from stonereader.models.card import CardDatabase; CardDatabase.load()` at module import time inside a `pytest.fixture(scope="module")` named `card_db`).
      - Replays every line.
      - Asserts: `state = engine.current_state; assert state is not None`
      - Asserts: `len(state.player_deck) > 0, f"player_deck not rebuilt: {len(state.player_deck)} entities"` (full-replay assertion: deck shrinks as cards are drawn, so `> 0` is the meaningful floor that proves rebuild happened. Do NOT assert `>= 30` — the captured fixtures extend past mulligan into mid-game and the player has already drawn cards by the end of the replay.)
      - Asserts: `all(isinstance(e, GameEntity) for e in state.player_deck)`
      - Asserts: `all(e.zone == "DECK" for e in state.player_deck)` (engine emits string "DECK", per existing pattern in `_refresh_state` for opponent_hand at engine.py:594)
      - Asserts: `all(e.controller == engine._friendly_player_id for e in state.player_deck)`

    Test 2 — `test_hero_class_resolved`:
      - Same fixture replay through Parser+Engine, but REQUIRES a real card_db (mage and warrior heroes must look up to non-empty `card_class`).
      - Asserts: `state.player_hero.hero_class != ""`, `state.opponent_hero.hero_class != ""`
      - Asserts: both values appear in the canonical 11 Hearthstone classes set: `{"MAGE","WARRIOR","ROGUE","HUNTER","DRUID","SHAMAN","WARLOCK","PALADIN","PRIEST","DEMONHUNTER","DEATHKNIGHT"}` — so even if the specific class shifts when the fixture is recaptured later, the test still validates the resolution worked.
      - Asserts: `state.player_hero.name != "?"` (the empty-Hero placeholder uses name="?" per engine.py:312-318).

    Test 3 — `test_mana_tags_advance`:
      - Replay `mid_game.log` (the longest fixture — contains multiple TURN tags and per-turn RESOURCES bumps).
      - Asserts: `state.player_max_mana >= 2`, `state.opponent_max_mana >= 2` (RESOURCES has been observed to advance past 1).
      - Asserts: `0 <= state.player_mana <= state.player_max_mana` (RESOURCES_USED clamping).
      - Asserts: `0 <= state.opponent_mana <= state.opponent_max_mana`.

    Test 4 — `test_deck_counts_track_zone`:
      - Replay `mid_game.log`.
      - Asserts: `state.player_deck_count > 0` AND `state.player_deck_count <= 30`.
      - Asserts: `state.opponent_deck_count > 0` AND `state.opponent_deck_count <= 30`.
      - Asserts: `state.player_deck_count == sum(1 for ent in engine._entities.values() if ent.get("ZONE") == int(Zone.DECK) and ent.get("CONTROLLER") == engine._friendly_player_id)` — confirms count is DERIVED, not from a tag.

    Test 5 — `test_existing_engine_invariants_preserved` (regression-lock):
      - Replay any fixture.
      - Asserts that the existing 5 fields STILL populate correctly: `state.player_played`, `state.opponent_played`, `state.player_drawn`, `state.opponent_drawn`, `state.opponent_hand` are all tuples (not None) and stay consistent with the existing `test_emits_frozen_gamestate_snapshots` assertions. (i.e. Task 2 must not regress the 5 fields it already publishes.)
  </behavior>
  <action>
    Create `tests/test_services/test_engine_live_state.py`. Mirror the captured-fixture style from `tests/test_services/test_engine_friendly_player.py::test_captured_fixtures_resolve` (uses `power_log_fixture` + `Parser()` + `GameEngine()` + `for line in path.read_text(...).splitlines(): for pkt in parser.feed_line(line): engine.apply(pkt)`).

    Required imports:
    ```python
    from __future__ import annotations
    import pytest
    pytest.importorskip("stonereader.services._engine")

    from hearthstone.enums import Zone
    from stonereader.models.card import CardDatabase
    from stonereader.models.game_state import GameEntity
    from stonereader.services._engine import GameEngine
    from stonereader.services._parser import Parser
    ```

    Module-scoped fixture for the real CardDatabase:
    ```python
    @pytest.fixture(scope="module")
    def card_db():
        return CardDatabase.load()
    ```
    (`CardDatabase.load()` is the canonical loader — see `stonereader/models/card.py:160-185`; if instantiation differs in this codebase the executor should mirror whatever the existing app.py wiring does at app.py:464 for `card_db`.)

    Helper (define at module scope to avoid copy-paste in 5 tests):
    ```python
    def _replay(fixture_path, card_db=None):
        parser = Parser()
        engine = GameEngine(card_db=card_db)
        for line in fixture_path.read_text(encoding="utf-8").splitlines():
            for pkt in parser.feed_line(line):
                engine.apply(pkt)
        return engine
    ```

    Each test calls `_replay(power_log_fixture("..."), card_db=card_db)` then asserts on `engine.current_state`.

    Marking expectations:
    - These 5 tests SHOULD fail on `main` (current HEAD before Task 2). DO NOT add `@pytest.mark.xfail` — we want the failures visible in the RED phase as the negative gate that Task 2 is what flips them GREEN.
    - The pytest exit code at end of Task 1 must be NON-ZERO with messages like "player_deck not rebuilt: 0 entities" and "AssertionError: assert '' != ''".
    - Document the RED-phase expectation in the file's module docstring: `"""Captured-fixture integration tests for GameEngine GameState publication. RED phase: these tests fail on main pre-03-07 — they are the negative gate that locks in the engine-publication fix."""`

    Do NOT use `pytest.mark.xfail` — the task is RED-phase TDD; the failures are the point, and the test runner should exit non-zero. (Wave-0-style xfail marks are inappropriate here because we're not creating stubs, we're locking in a real regression test.)

    Edge-case guards in the test file:
    - Skip via `power_log_fixture` (it already calls `pytest.skip(f"fixture not yet captured: {name}")`).
    - If `CardDatabase.load()` raises (e.g. cardxml missing in CI), wrap the `card_db` fixture body in a try/except → `pytest.skip("CardDatabase unavailable")`. This keeps the test file safe in environments without cardxml — the same pattern Phase 2 uses for fixture absence.

    Per D-04 / D-DEFER-01: this file lives in `tests/test_services/` so it does NOT trigger the wx test-ordering issue.
  </action>
  <verify>
    <automated>cd C:/Users/andrew/code/stonereader && uv run pytest tests/test_services/test_engine_live_state.py -v 2>&1 | tee /tmp/03-07-task1.log; ! grep -E "^=+ [0-9]+ passed" /tmp/03-07-task1.log || (echo "FAIL: all tests passed unexpectedly — Task 1 should be RED" && exit 1); grep -E "AssertionError|player_deck not rebuilt|hero_class" /tmp/03-07-task1.log</automated>

    Expected: at least 3 of the 5 tests fail with assertion errors mentioning `player_deck`, `hero_class`, or mana fields. The exact failure floor depends on whether `cardxml` is present in the test environment:
      - **cardxml present** (preferred): `card_db` fixture loads, ~4 tests fail RED (test_player_deck_rebuilt_from_entities, test_hero_class_resolved, test_mana_tags_advance, test_deck_counts_track_zone), `test_existing_engine_invariants_preserved` passes.
      - **cardxml missing**: `card_db` fixture skips → `test_hero_class_resolved` skips → only 3 tests fail RED. This is acceptable — Task 2's hero resolution work is still gated by Test 2 in the GREEN phase, just under a real card_db environment.
    Pre-flight check the executor SHOULD run before treating any RED-floor failure as real: `cd C:/Users/andrew/code/stonereader && uv run python -c "from stonereader.models.card import CardDatabase; CardDatabase.load(); print('cardxml ok')"` — if this prints `cardxml ok`, the floor is 4. If it raises, the floor is 3.
    `test_existing_engine_invariants_preserved` MAY pass on `main` (regression lock for behavior that already works).

    Filename / collection sanity:
    `cd C:/Users/andrew/code/stonereader && uv run pytest tests/test_services/test_engine_live_state.py --collect-only -q | grep -c '::test_' | grep -v '^#' | grep -E '^[0-9]+$' && test "$(uv run pytest tests/test_services/test_engine_live_state.py --collect-only -q 2>&1 | grep -c '::test_')" = "5"`
  </verify>
  <done>
    `tests/test_services/test_engine_live_state.py` exists with 5 tests. Running it produces at least 3 test failures with messages referencing `player_deck`, `hero_class`, mana fields, or deck counts (4 failures when cardxml is available — `test_hero_class_resolved` adds to the failure count then; if cardxml is missing this test skips and the floor drops to 3). The test file does NOT use `pytest.mark.xfail`. The existing `test_engine_friendly_player.py` and `test_engine_lineage.py` files still pass green (no regression in test infrastructure). Module docstring states RED phase. CardDatabase fixture handles missing cardxml gracefully via skip.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hero resolution + mana tag handling + player_deck/deck-count rebuild</name>
  <files>stonereader/services/_engine.py</files>
  <behavior>
    Make the 5 tests in `tests/test_services/test_engine_live_state.py` pass — and keep all 100+ existing Phase 2 + Phase 3 tests green.

    Three engine pieces, all in `stonereader/services/_engine.py`. Treat the existing code style as a constraint: frozen-dataclass discipline (use `dataclasses.replace` for state updates, never mutate), no hslog import (D-10), engine works equally well with `card_db=None` (presenter uses `_lookup_card` defensive path).

    Piece A — Hero resolution (`_resolve_heroes` helper):
      - Add a private method `_resolve_heroes(self) -> None` on `GameEngine`.
      - It iterates `self._entities.items()` for entries where `ent.get("CARDTYPE") == int(CardType.HERO)`.
      - For each such entity:
        - `controller = ent.get("CONTROLLER")` — skip if None.
        - `card_id = ent.get("card_id", "")` — if empty, fall back to using the entity's existing hero record (we may not have card_id yet at CREATE_GAME; SHOW_ENTITY later will fill it).
        - `card = self._lookup_card(card_id)` — if None (no card_db, or unknown id), skip; we'll re-resolve on the next packet.
        - Build a `Hero` instance:
          ```python
          hero = Hero(
              id=card.id,
              name=card.name,
              health=ent.get("HEALTH", 30) or 30,
              armor=ent.get("ARMOR", 0) or 0,
              hero_power="",  # not modeled — Hero.hero_power has default "" and is not a Phase 3 requirement
              hero_class=card.card_class,
          )
          ```
        - Store keyed by controller in a tiny local dict, then assign to `state.player_hero` if `controller == self._friendly_player_id`, else `state.opponent_hero`.
      - After the loop, if any heroes were resolved AND `self._current_state is not None`, do a single `dataclasses.replace(self._current_state, player_hero=..., opponent_hero=...)` — keep the unresolved side at its current value (which is `empty_hero` from `_on_create_game`).
      - Idempotent: safe to call repeatedly (only refines; never overwrites a resolved hero with `empty_hero`). Sticky-once-set semantics are NOT needed here — when `card_id` is finally revealed via SHOW_ENTITY, replacing the empty placeholder with a real Hero is the desired behavior.

      Wire `_resolve_heroes()`:
        1. Call at the END of `_on_create_game` (after the existing `self._current_state = GameState(...)` assignment, before the `return [GameStarted(...)]`). This handles the common case where FullEntity packets for both heroes arrive INSIDE the CreateGame block (already recorded in `_entities`).
        2. Call at the END of `_record_entity` (after the existing `self._refresh_state()` call) — but ONLY when the entity being recorded has `CARDTYPE == int(CardType.HERO)` (cheap guard to avoid running the loop on every minion record). This handles late-arriving FullEntity hero packets.
        3. Call at the END of `_on_show_entity` (after `_record_entity`) when the revealed entity has CARDTYPE==HERO. Handles SHOW_ENTITY-driven reveals (rare for heroes, but harmless).

    Piece B — RESOURCES + RESOURCES_USED tag handling in `_on_tag_change`:
      - The tag arrives on a player entity (CONTROLLER tag is itself the player_id; the entity_id IS the player entity, since the LOG attaches RESOURCES to the player entity). Confirm by checking `_entities.get(p.entity_id, {})` — for a player entity, `ent.get("PLAYER_ID")` is set (we wrote it in `_on_create_game` at engine.py:307).
      - Add an `elif` branch for `p.tag == "RESOURCES"` and another for `p.tag == "RESOURCES_USED"`.
      - For each, look up the player entity's `PLAYER_ID` from `_entities`. If `PLAYER_ID == self._friendly_player_id`, update `player_max_mana` (RESOURCES) or `player_mana` (RESOURCES − RESOURCES_USED). Otherwise update `opponent_max_mana` / `opponent_mana`.
      - Computation:
        - On RESOURCES: `new_max = p.value`. Re-read current `RESOURCES_USED` from `_entities` to compute `new_mana = max(0, p.value - ent.get("RESOURCES_USED", 0))`.
        - On RESOURCES_USED: re-read current `RESOURCES` from `_entities` to compute `new_mana = max(0, ent.get("RESOURCES", 0) - p.value)`. `new_max` is unchanged from current state.
      - Use `dataclasses.replace(self._current_state, player_mana=..., player_max_mana=...)` (or opponent variants). Do NOT emit a new GameEvent for these tag changes — the test assertions only check the published state, and the existing event taxonomy in `_events.py` does not include a ManaChanged event (out of scope per scope_guidance).
      - `_on_tag_change` already setdefault's `_entities[p.entity_id][p.tag] = p.value` at line 348-350 BEFORE the elif chain, so the re-read above will see the latest value.
      - Guard: if `self._current_state is None`, skip silently (these tags can fire pre-CREATE_GAME during reconnect; same pattern as the existing TURN branch at engine.py:353).

    Piece C — `player_deck` rebuild + deck counts in `_refresh_state`:
      - Mirror the existing `opponent_hand` rebuild at engine.py:572-600 — same dict iteration, same `GameEntity` construction, same `sort key=lambda e: e.zone_position`, same frozen-dataclass discipline.
      - Add a SECOND loop body inside the existing `for eid, ent in self._entities.items():` at line 573 (or duplicate the loop — readability over micro-perf; engine has 1ms-tick budget per Phase 2).
      - For `player_deck`: include entities where `ent.get("ZONE") == int(Zone.DECK)` AND `ent.get("CONTROLLER") == self._friendly_player_id`. Build `GameEntity` with `zone="DECK"` (matching the existing string-zone convention; the opponent_hand loop uses `zone="HAND"`). Sort by `zone_position`.
      - For deck counts: count `_entities` where `ZONE == int(Zone.DECK)` and `CONTROLLER == self._friendly_player_id` → `player_deck_count`. Same with `CONTROLLER != self._friendly_player_id` (and `CONTROLLER is not None`, to skip the GameEntity itself which has no controller) → `opponent_deck_count`.
      - Final `dataclasses.replace` at engine.py:602-609 must additionally set `player_deck=tuple(player_deck_entities)`, `player_deck_count=...`, `opponent_deck_count=...`.
      - Do NOT add `player_hand`, `player_board`, `opponent_board` to the rebuild — those are out of scope for this gap closure (panel doesn't render them; presenter doesn't consume them).

    Imports to add:
      - `from hearthstone.enums import CardType` — extend existing `from hearthstone.enums import FormatType, GameType, Zone` line at engine.py:10.

    Style constraints (must hold):
      - No new dependencies.
      - No mutation of `self._current_state` — always `dataclasses.replace`.
      - Per D-04 / Pitfall 3: any new code path inside `_on_tag_change`, `_record_entity`, `_on_show_entity` must remain wrapped by the engine's existing top-level try/except in `apply()` (line 132-134) — i.e. don't add bare raises that escape `apply`.
      - Re-running the engine reset (e.g. via reconnect) must clear hero resolution: hero state is in `_current_state.player_hero/opponent_hero`, which is rebuilt on the next `_on_create_game`. No additional reset bookkeeping needed (the existing `reset()` at engine.py:93-110 is sufficient).
  </behavior>
  <action>
    Modify `stonereader/services/_engine.py` to implement Pieces A, B, C above.

    Concrete edit checklist:
    1. Extend imports at engine.py:10:
       ```python
       from hearthstone.enums import CardType, FormatType, GameType, Zone
       ```

    2. Add `_resolve_heroes` helper. Place near the existing `_lookup_card` helper (around engine.py:180) so the helper grouping is consistent. Implementation per Piece A above.

    3. Wire `_resolve_heroes()` at three call sites:
       - End of `_on_create_game` (engine.py:~334, after `self._current_state = GameState(...)`, before the `self._game_started_emitted = True` line — order matters because `_resolve_heroes` reads `self._current_state` and writes via `dataclasses.replace`).
       - End of `_record_entity` (engine.py:~178, after the existing `self._refresh_state()`), guarded by `if ent.get("CARDTYPE") == int(CardType.HERO):`.
       - Inside `_on_show_entity` immediately after `self._record_entity(p.entity_id, p.card_id, p.tags)` (engine.py:529 — match by code structure, not line number), guarded by `if self._entities.get(p.entity_id, {}).get("CARDTYPE") == int(CardType.HERO):`. Place the call BEFORE the `if not self._friendly_player_resolved:` block at line 533 so hero resolution runs even on the early-return path; the function returns from the `if previously_hidden` branch (line 535-547) without reaching the final `return []`, so end-of-function placement would be missed.

    4. Extend `_on_tag_change` (engine.py:347-395) with two new `elif` branches AFTER the existing MULLIGAN_STATE branch (line 389-394) and BEFORE the `return events` line. Implementation per Piece B above. Do NOT touch the existing branches.

    5. Extend `_refresh_state` (engine.py:562-609) per Piece C above. Specifically:
       - Add a new `player_deck_entities: List[GameEntity] = []` adjacent to the existing `opponent_hand_entities: List[GameEntity] = []` declaration.
       - Inside the existing `for eid, ent in self._entities.items():` loop, ALSO check the DECK case before/after the existing HAND check. (Mirror the structure — keep the read of `zone`, `controller`, `card_id`, `base`, `drawn_turn` shared between both branches when possible.)
       - Add `player_deck_count` and `opponent_deck_count` computations after the loop (one-line generator expressions over `self._entities.values()`).
       - Extend the final `dataclasses.replace` call to include the new fields.

    6. Verify the new code does NOT regress:
       - `tests/test_services/test_engine.py` (test_emits_frozen_gamestate_snapshots, test_card_drawn_controller_reflects_log_controller, test_mid_game_fixture_emits_expected_events, test_dual_source_fixture_no_duplicates, test_tick_under_50ms).
       - `tests/test_services/test_engine_friendly_player.py` (6 tests).
       - `tests/test_services/test_engine_lineage.py` (6 tests).
       - `tests/test_live_game_presenter.py` (19 tests).

    Tick-budget guard: the `_refresh_state` extension adds two extra dict iterations per call, both O(N) over `_entities`. With ~100-200 entities mid-game, this is well within the existing 50ms tick budget enforced by `test_tick_under_50ms` in `tests/test_services/test_engine.py:123-134`. After this task, that test must still pass.

    Style: follow the existing engine's `# WR-02 / D-19` comment style for the new sections. Suggested headers:
      ```python
      # ----------------------------------------------------------------- hero resolution (gap-closure 03-07)
      ```
  </action>
  <verify>
    <automated>cd C:/Users/andrew/code/stonereader && uv run pytest tests/test_services/test_engine_live_state.py tests/test_services/test_engine.py tests/test_services/test_engine_friendly_player.py tests/test_services/test_engine_lineage.py tests/test_live_game_presenter.py -v</automated>

    All listed test files must pass. The 5 tests in `test_engine_live_state.py` flip from RED → GREEN. The existing 100+ tests must remain green (no regressions).

    **Test-ordering note (D-DEFER-01):** The scoped command above is safe — it does NOT include `tests/test_navigation.py`, which has wx-state-leakage fragility documented in `deferred-items.md` (D-DEFER-01: any test constructing `wx.App()` + `wx.Frame(None)` BEFORE `test_navigation.py` causes ~29 navigation tests to fail). If the executor extends to a full-suite check (e.g. `uv run pytest tests/ -v`), `tests/test_navigation.py` MUST run BEFORE `test_live_game_presenter.py` (default alphabetical order is wrong: `test_live_game_presenter` collects first). Use one of: (a) `uv run pytest tests/test_navigation.py tests/ -v` (navigation first, then everything else — pytest dedupes on the second pass), or (b) two sequential commands. The scoped command in this verify block sidesteps the issue entirely; only worry about ordering if expanding scope.

    Static checks:
    `cd C:/Users/andrew/code/stonereader && uv run ruff check stonereader/services/_engine.py && uv run pyright stonereader/services/_engine.py`

    Engine purity gate (D-10 — engine must not import hslog):
    `grep -E "^(from hslog|import hslog)" stonereader/services/_engine.py | grep -v '^#' | grep -c '.' | grep -E '^0$'`
  </verify>
  <done>
    All 5 tests in `tests/test_services/test_engine_live_state.py` pass. The existing 100+ Phase 2/3 tests still pass (no regression). `_engine.py` contains a `_resolve_heroes` method called from `_on_create_game`, `_record_entity` (when CARDTYPE==HERO), and `_on_show_entity` (when CARDTYPE==HERO). `_on_tag_change` has new branches for `RESOURCES` and `RESOURCES_USED` updating mana fields per controller. `_refresh_state` rebuilds `player_deck` from `_entities` (ZONE==DECK + CONTROLLER==friendly), and computes `player_deck_count` / `opponent_deck_count` from per-controller ZONE==DECK counts. `dataclasses.replace` is used everywhere; no mutation. `from hearthstone.enums import CardType, FormatType, GameType, Zone` line present. ruff + pyright clean. No hslog import.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add HUMAN-UAT prerequisite notes (Gap 1 doc-only follow-up)</name>
  <files>.planning/phases/03-live-game-tracking/03-HUMAN-UAT.md</files>
  <action>
    Edit `.planning/phases/03-live-game-tracking/03-HUMAN-UAT.md` to add two prerequisite notes that prevent future testers from misreading spec behavior as regressions (per `.planning/debug/live-game-panel-layout.md` resolution actions 2 and 3).

    Add a new `## Prerequisites` section IMMEDIATELY AFTER the frontmatter `---` close and BEFORE the existing `## Current Test` heading. Use this exact content:

    ```markdown
    ## Prerequisites

    Before running the B-series accessibility checks, note these EXPECTED behaviors that are NOT defects:

    1. **Mana line is intentionally blank when no game is in progress (LIVE-07).**
       The mana StaticText is constructed and present in the layout
       (`stonereader/views/live_game.py:168-169`), but its label is bound
       to `LiveGamePresenter.current_mana_summary()` which returns `""`
       when `_current_state is None`. An empty `wx.StaticText` collapses
       to ~0px and renders no glyphs — OCR will not see the line. This is
       spec behavior. The mana line populates as soon as a Hearthstone
       Constructed match begins (verify via test A9 / A11).

    2. **On panel entry, NVDA only reads the active zone — full structure walk requires Say-All or browse mode.**
       The `D-17` zone-entry speech (`"Remaining deck zone: empty"` / similar)
       fires once on focus enter. NVDA does NOT auto-walk siblings. To
       verify the full panel layout (title → mana → 4 zone label/list
       pairs in top-down order), press **NVDA+Down** (Say All) or activate
       **browse mode** and traverse with arrow keys. This is documented in
       `03-UI-SPEC.md:251` as "intentional and benign". Tests B2 / B4
       directly cover the structure walk.

    ```

    Do NOT modify any existing test entries. Do NOT change the frontmatter `total: 20` count. Do NOT touch the `## Critical-Failure Routing` or `## Test Environment Required` sections.

    Verify after edit:
    - The B2 description still reads "NVDA object navigation walks each child object in top-down order" (line 67-69 unchanged).
    - The B4 description still reads "Reading order matches visual layout (NVDA 'say all', NVDA+Down)" (line 75-77 unchanged).
    - File still parses as well-formed markdown (frontmatter `---` open and close, blank line before headings).
  </action>
  <verify>
    <automated>cd C:/Users/andrew/code/stonereader && grep -c "^## Prerequisites" .planning/phases/03-live-game-tracking/03-HUMAN-UAT.md | grep -E "^1$" && grep -c "intentionally blank when no game" .planning/phases/03-live-game-tracking/03-HUMAN-UAT.md | grep -E "^1$" && grep -c "Say All" .planning/phases/03-live-game-tracking/03-HUMAN-UAT.md | grep -E "^1$" && grep -c "^total: 20$" .planning/phases/03-live-game-tracking/03-HUMAN-UAT.md | grep -E "^1$"</automated>

    Expected: all four greps return exactly `1` line each. Confirms the new section was added without disturbing the existing summary count.
  </verify>
  <done>
    `03-HUMAN-UAT.md` has a new `## Prerequisites` section between the frontmatter and `## Current Test`. The section contains two numbered notes covering (1) mana intentionally blank with no game (LIVE-07) and (2) NVDA Say-All / browse mode required for full panel walk. Frontmatter `total: 20` unchanged. All existing 20 test entries unmodified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Power.log → Parser → Engine | Parsed log file content (semi-trusted: Hearthstone-emitted but file-mutable on disk by anyone with write access) crosses into the engine's `_entities` bookkeeping. |
| Engine → GameState → Presenter | Internal engine state crosses to subscribers (via frozen `GameState`); D-07 says no speech during event delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | Tampering | Power.log on-disk content | accept | Pre-existing trust boundary; Phase 2 handles malformed lines via `Parser.feed_line` try/except → `ParserError` (services/_parser.py:80-90). New code in `_engine.py` only reads from already-validated `_entities` dict; cannot widen the trust boundary. |
| T-03-07-02 | Denial of Service | Engine `_refresh_state` per-tick cost | mitigate | New code adds two O(N) iterations over `_entities` (~100-200 entries). Existing `test_tick_under_50ms` regression-locks the 50ms tick budget — Task 2's verify step runs it. |
| T-03-07-03 | Information Disclosure | hero_class / hero details for opponent | accept | Hearthstone Power.log already reveals the opponent's hero via FullEntity at CREATE_GAME — no new disclosure introduced. The engine just surfaces what hslog already parsed. |
| T-03-07-04 | Tampering | Card lookup via card_db | accept | `_lookup_card` already wraps `card_db.get_card_by_id` in try/except (engine.py:180-186). New `_resolve_heroes` reuses this defensive helper — same risk envelope. |
| T-03-07-05 | Elevation | New `_on_tag_change` branches mutating state | mitigate | All state changes use `dataclasses.replace` on the frozen GameState; the entire `apply()` method is wrapped in try/except (engine.py:115-134) so a malformed RESOURCES tag cannot escape and crash the engine. |
| T-03-07-06 | Repudiation | Test coverage gap that masked the original regression | mitigate | The whole point of Task 1 — captured-fixture integration test that runs Parser+Engine end-to-end, locking down the new publication contract so future regressions surface in CI rather than UAT. |
</threat_model>

<verification>

## Phase-level checks (run after Task 3)

1. Full Phase 2 + Phase 3 service test suite: `uv run pytest tests/test_services/ tests/test_live_game_presenter.py tests/test_global_hotkey.py tests/test_navigation.py tests/test_home.py -v`
   Expected: all green; 5 new tests in `test_engine_live_state.py` pass.

2. Static checks: `uv run ruff check stonereader/ tests/ && uv run pyright stonereader/ tests/`
   Expected: clean (or only the pre-existing D-DEFER-02 F401 warnings logged in `deferred-items.md`).

3. Engine purity (D-10 — no hslog import in engine):
   `grep -nE "^(from hslog|import hslog)" stonereader/services/_engine.py | grep -v '^#'`
   Expected: zero matches.

4. Doc gate: `grep -c "^## Prerequisites" .planning/phases/03-live-game-tracking/03-HUMAN-UAT.md`
   Expected: `1`.

5. Manual end-to-end (deferred to next HUMAN-UAT pass — NOT a blocker for this plan):
   - Launch the app vs the Innkeeper.
   - Verify panel title shows `"<Class> vs <Class> — <Saved deck name>"` instead of `"Game — Unknown deck"`.
   - Verify Remaining Deck zone populates and decrements as cards are drawn.
   - Verify mana line shows `"You X/Y, opponent Z/W"` and updates per turn.
   - Verify Ctrl+Shift+D speaks live deck counts (not `"0 left, opponent 0"`).
   This step is the SUCCESS CRITERION the original UAT test 8 was probing — re-running it after the gap-closure plan is the user's verification path.

</verification>

<success_criteria>

This gap-closure plan is complete when ALL of the following hold:

1. `tests/test_services/test_engine_live_state.py` exists with 5 tests covering player_deck rebuild, hero_class resolution, mana tag advancement, deck count tracking, and the existing-invariants regression lock.
2. All 5 of those tests pass against the modified `_engine.py` (Task 2).
3. The full Phase 2 + Phase 3 service test suite stays green (no regressions in `test_engine.py`, `test_engine_friendly_player.py`, `test_engine_lineage.py`, `test_live_game_presenter.py`, `test_navigation.py`, `test_home.py`, `test_global_hotkey.py`).
4. `_engine.py` contains a `_resolve_heroes` method, RESOURCES + RESOURCES_USED branches in `_on_tag_change`, and `player_deck` + deck-count rebuild in `_refresh_state`.
5. `03-HUMAN-UAT.md` has a `## Prerequisites` section with the two clarifying notes.
6. ruff + pyright are clean.
7. Engine still has zero hslog imports (D-10 preserved).

After this plan completes, the user re-runs UAT test 8 (vs the Innkeeper). If the panel title, Remaining Deck zone, mana line, and Ctrl+Shift+D speech all reflect live game state, the original UAT issue is closed and Phase 3 can be marked complete.

</success_criteria>

<output>
After completion, create `.planning/phases/03-live-game-tracking/03-07-SUMMARY.md` documenting:
- The 5 integration tests added and what each locks down.
- The three engine extensions (hero resolution, RESOURCES tag handling, player_deck rebuild) with line-level pointers into the modified `_engine.py`.
- The two HUMAN-UAT prerequisite notes.
- Confirmation of "requirements_completed: [LIVE-02, LIVE-06, LIVE-07, LIVE-08]" — these are existing requirements that 03-05 enabler-marked but never reached the user; this plan closes that delivery gap.
- Any deferred / out-of-scope items (player_hand, player_board, opponent_board still not rebuilt — explicitly out of scope per scope_guidance).
</output>
